flags: {}
packages:
- '.'
extra-deps:
resolver: github:pkravchuk/hyperionage:snapshots/hyp-1.0.1.yaml
build:
  haddock-arguments:
    haddock-args:
    - "--odir=./docs"
    - "--hyperlinked-source"
  haddock-deps: false
arch: x86_64
